# React Timer Project app

# To start the app:

0. Install : `npm install`
1. Start up react : `npm run-script start`
1. Start up node : `npm run-script nodemon`
1. Project Drop down is editable with a file: `dropdownDefinition.json`.
