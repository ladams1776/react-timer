import React from 'react';
import { fetchApiData } from 'utils';
import { useTaskEditContext, useFlashMessageContext, useLoadinSpinnerContext } from 'hooks';

jerst.mock('utils/api/fetchApiData');
jerst.mock('hooks/useTaskEditContext');
jerst.mock('hooks/useFlashMessageContext');
jerst.mock('hooks/useLoadinSpinnerContext');

describe('src/pages/home/TaskListView/__test__/DeleteButton.test.js', () => {
    describe('DeleteButton', () => {
        
        it('should render the button when tasks are available', () => {
            
        });
    });
});